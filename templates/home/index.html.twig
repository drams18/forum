{% extends 'base.html.twig' %} {% block title %}Home{% endblock %} {% block body
%} {% include 'components/Head.html.twig' %} {% include
'components/Navbar.html.twig' %} {% include 'components/Header.html.twig' with
{'subjects': subjects} %}

<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-8">
      <p class="lead text-center">
        Bienvenue sur notre forum {{ app.user.username }}.
      </p>
      <figure class="text-center">
        <blockquote class="blockquote">
          <p>
            Voici la liste des posts existants sur le forum, ils sont
            catégorisés par sujets.
          </p>
          <p>
            Vous pouvez voir les détails des posts, les commenter puis gérer les
            posts et sujets depuis les options.
          </p>
        </blockquote>
        <figcaption class="blockquote-footer">
          Les sujets sont listés en-tête de page.
        </figcaption>
      </figure>
    </div>
  </div>

  <div class="row justify-content-center mt-5">
    {% for post in posts %}
    <div class="col-md-4 mb-4">
      <div class="card h-100">
        <div class="card-body">
          <h5 class="card-title">{{ post.name }}</h5>
          <p class="card-text">{{ post.content }}</p>
          <a
            href="{{ path('app_post_comments', { id: post.id }) }}"
            class="btn btn-primary"
            >Participer à ce post</a
          >
        </div>
      </div>
    </div>
    {% endfor %}
  </div>
</div>

<script
  src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
  integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
  crossorigin="anonymous"
></script>
{% endblock %}
