{% extends 'base.html.twig' %} {% block title %}Liste des sujets{% endblock %}
{% block body %}

{% include 'components/Head.html.twig' %} 
{% include 'components/Navbar.html.twig' %} 
{% include 'components/Header.html.twig' with {'subjects': subjects} %}

<div class="container">
  <h2>Liste des sujets existants</h2>
  <a href="{{ path('app_subject_create') }}" class="btn btn-secondary"
    >Créer un sujet</a
  >
  <ul>
    <table class="table">
      <thead>
        <tr>
          <th scope="col">#</th>
          <th scope="col">Nom du Sujet</th>
          <th scope="col">Description</th>
          {# <th scope="col">Action</th> #}
        </tr>
      </thead>
      <tbody>
        {% for subject in subjects %}
        <tr>
          <th scope="row">{{ subject.id }}</th>
          <td>{{ subject.name }}</td>
          <td>{{ subject.description }}</td>
          {# <td>
            <div class="d-flex">
              <form
              method="get"
              action="{{ path('app_subject_edit_single', { id: subject.id }) }}"
            >
              <button type="submit" class="btn btn-primary">Modifier</button>
            </form>
              <form
                method="post"
                action="{{ path('app_subject_delete', { id: subject.id }) }}"
              >
                <input
                  type="hidden"
                  name="subject_id"
                  value="{{ subject.id }}"
                />
                <button type="submit" class="btn btn-danger"> Supprimer</button>
              </form>
            </div>
          </td> #}
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </ul>
</div>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>

{% endblock %}
